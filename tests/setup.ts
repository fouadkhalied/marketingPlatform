// Jest setup file
import 'reflect-metadata';

// Mock environment variables for testing
process.env.NODE_ENV = 'test';
process.env.LOG_LEVEL = 'debug';

// Mock any external dependencies here if needed
